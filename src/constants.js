export const DEFAULT_HTML_CODE = `<h1>Get started with PLayJS</h1>
<p>Write some code on the left to see the output here !</p>`;
export const DEFAULT_CSS_CODE = `@import url('https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&display=swap');

*, *::before, *::after {
    margin: 0;
    padding: 0;
}

body {
    height: 100vh;
    font-family: 'Bricolage Grotesque';
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 1rem;
}
h1 {
    color: rgb(69, 154, 219);
}


`;
export const DEFAULT_JS_CODE = `/* Write your code from here...*/\nconsole.log('Write some epic JS !;)')`;
